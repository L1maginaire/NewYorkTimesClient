package com.example.guest.newyorktimesclient.di.modules

import android.content.Context
import dagger.Module
import dagger.Provides
import javax.inject.Named
import javax.inject.Singleton

@Module
class ImageLoaderModule {

//    @Singleton
//    @Provides
//    fun provideImageLoader(@Named("app_context") context: Context, netUtils: NetUtils, dispatchersProvider: DispatchersProvider) = ImageLoader(context, netUtils, dispatchersProvider)
}